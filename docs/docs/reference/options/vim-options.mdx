---
title: vim-options
sidebar_label: vim-options
sidebar_position: 15
description: Options set by conf/vim-options.nix
---

# `conf/vim-options.nix`

{/* This page is generated by docs/scripts/render-options.ts. Do not edit it by hand. */}

This page is generated from a Nix evaluation of the configuration. It lists the 7 options that [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix) sets.

### `vim.options`

A freeform attribute set containing vim options to be set as early as possible.
If populated, this option will set vim options in the built \{option\}`luaConfigRC`
after `basic` and before `pluginConfigs` DAG entries.

::: \{.note\}
`\{foo = "bar";\}` will set `vim.opt.foo` to "bar", where the type of
`bar` in the resulting Lua value will be inferred from the type of the
value in the `\{name = value;\}` pair passed to the option.
:::


<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>open submodule of attribute set of anything</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
{ }
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
{
  autoindent = true;
  backup = false;
  clipboard = "";
  cmdheight = 1;
  confirm = true;
  cursorlineopt = "line";
  encoding = "utf-8";
  errorbells = false;
  expandtab = true;
  foldcolumn = "1";
  foldenable = true;
  foldlevel = 99;
  foldlevelstart = 99;
  hidden = true;
  mouse = "nvi";
  mousemoveevent = true;
  number = true;
  relativenumber = true;
  scrolloff = 8;
  shada = "!,'100,<50,s10,h";
  shiftwidth = 2;
  signcolumn = "yes";
  softtabstop = 2;
  spell = true;
  spelllang = "en";
  splitbelow = true;
  splitright = true;
  swapfile = false;
  tabstop = 2;
  termguicolors = true;
  tm = 500;
  undodir = mkLuaInline ''
    vim.fn.stdpath('state') .. '/undo'
  '';
  undofile = true;
  updatetime = 300;
  visualbell = false;
  wrap = true;
  writebackup = false;
}
```

### `vim.searchCase`

Set the case sensitivity of search

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>one of "ignore", "smart", "sensitive"</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>sensitive</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
"smart"
```

### `vim.statusline.lualine.enable`

Whether to enable lualine statusline plugin.

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>false</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix), [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
true
```

### `vim.theme.enable`

Enable theming

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
true
```

### `vim.theme.name`

Supported themes can be found in \{file\}`supportedThemes.nix`.
Setting the theme to "base16" enables base16 theming and
requires all of the colors in \{option\}`vim.theme.base16-colors` to be set.


<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>one of "base16", "catppuccin", "dracula", "everforest", "github", "gruber-darker…</code></span>

<details class="option-type-details">
<summary>Show full type</summary>

```
one of "base16", "catppuccin", "dracula", "everforest", "github", "gruber-darker", "gruvbox", "mellow", "mini-base16", "nord", "onedark", "oxocarbon", "rose-pine", "solarized", "solarized-osaka", "tokyonight"
```

</details>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
"gruvbox"
```

### `vim.theme.style`

Specific style for theme if it supports it

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>one of "dark", "light"</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
"dark"
```

### `vim.undoFile.enable`

Whether to enable undofile for persistent undo behaviour.

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>false</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
true
```
