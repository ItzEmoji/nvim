---
title: git
sidebar_label: git
sidebar_position: 2
description: Options set by conf/config/git.nix
---

# `conf/config/git.nix`

{/* This page is generated by docs/scripts/render-options.ts. Do not edit it by hand. */}

This page is generated from a Nix evaluation of the configuration. It lists the 2 options that [`conf/config/git.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/config/git.nix) sets.
### `vim.git.enable`

Whether to enable git integration suite.

Enabling this option will enable the following plugins:

* gitsigns
* hunk-nvim
* vim-fugitive
* git-conflict
* gitlinker-nvim
.

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>false</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/config/git.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/config/git.nix)</span>

</div>

**Value:**

```nix
true
```

### `vim.git.gitsigns.setupOpts`

Option table to pass into the setup function of gitsigns

You can pass in any additional options even if they're
not listed in the docs


<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>open submodule of anything</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>\{\}</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/config/git.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/config/git.nix)</span>

</div>

**Value:**

```nix
{
  current_line_blame = true;
  current_line_blame_formatter = "  <author>, <author_time:%R> · <summary>";
  current_line_blame_opts = {
    delay = 300;
    virt_text = true;
    virt_text_pos = "eol";
  };
}
```
