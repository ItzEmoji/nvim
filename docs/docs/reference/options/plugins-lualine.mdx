---
title: lualine
sidebar_label: lualine
sidebar_position: 9
description: Options set by conf/plugins/lualine.nix
---

# `conf/plugins/lualine.nix`

{/* This page is generated by docs/scripts/render-options.ts. Do not edit it by hand. */}

This page is generated from a Nix evaluation of the configuration. It lists the 11 options that [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix) sets.

### `vim.statusline.lualine.activeSection.a`

active config for: | (A) | B | C       X | Y | Z |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  \"mode\",
  icons_enabled = true,
  separator = {
    left = '▎',
    right = ''
  },
}
"
  "{
  \"\",
  draw_empty = true,
  separator = { left = '', right = '' }
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[
  "{\"mode\"}"
]
```

### `vim.statusline.lualine.activeSection.b`

active config for: | A | (B) | C       X | Y | Z |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  \"filetype\",
  colored = true,
  icon_only = true,
  icon = { align = 'left' }
}
"
  "{
  \"filename\",
  symbols = {modified = ' ', readonly = ' '},
  separator = {right = ''}
}
"
  "{
  \"\",
  draw_empty = true,
  separator = { left = '', right = '' }
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[
  "{\"branch\"}, {\"diff\"}, {\"diagnostics\"}"
]
```

### `vim.statusline.lualine.activeSection.c`

active config for: | A | B | (C)       X | Y | Z |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  \"diff\",
  colored = false,
  diff_color = {
    -- Same color values as the general color option can be used here.
    added    = 'DiffAdd',    -- Changes the diff's added color
    modified = 'DiffChange', -- Changes the diff's modified color
    removed  = 'DiffDelete', -- Changes the diff's removed color you
  },
  symbols = {added = '+', modified = '~', removed = '-'}, -- Changes the diff symbols
  separator = {right = ''}
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[ ]
```

### `vim.statusline.lualine.activeSection.x`

active config for: | A | B | C       (X) | Y | Z |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  -- Lsp server name
  function()
    local buf_ft = vim.bo.filetype
    local excluded_buf_ft = { toggleterm = true, NvimTree = true, [\"neo-tree\"] = true, TelescopePrompt = true }

    if excluded_buf_ft[buf_ft] then
      return \"\"
      end

    local bufnr = vim.api.nvim_get_current_buf()
    local clients = vim.lsp.get_clients({ bufnr = bufnr })

    if vim.tbl_isempty(clients) then
      return \"No Active LSP\"
    end

    local active_clients = {}
    for _, client in ipairs(clients) do
      table.insert(active_clients, client.name)
    end

    return table.concat(active_clients, \", \")
  end,
  icon = ' ',
  separator = {left = ''},
}
"
  "{
  \"diagnostics\",
  sources = {'nvim_lsp', 'nvim_diagnostic', 'nvim_diagnostic', 'vim_lsp', 'coc'},
  symbols = {error = '󰅙  ', warn = '  ', info = '  ', hint = '󰌵 '},
  colored = true,
  update_in_insert = false,
  always_visible = false,
  diagnostics_color = {
    color_error = { fg = 'red' },
    color_warn = { fg = 'yellow' },
    color_info = { fg = 'cyan' },
  },
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[
  "{\"filetype\"}"
]
```

### `vim.statusline.lualine.activeSection.y`

active config for: | A | B | C       X | (Y) | Z |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  \"\",
  draw_empty = true,
  separator = { left = '', right = '' }
}
"
  "{
  'searchcount',
  maxcount = 999,
  timeout = 120,
  separator = {left = ''}
}
"
  "{
  \"branch\",
  icon = ' •',
  separator = {left = ''}
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[ ]
```

### `vim.statusline.lualine.activeSection.z`

active config for: | A | B | C       X | Y | (Z) |

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>list of string</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
[
  "{
  \"\",
  draw_empty = true,
  separator = { left = '', right = '' }
}
"
  "{
  \"progress\",
  separator = {left = ''}
}
"
  "{\"location\"}
"
  "{
  \"fileformat\",
  color = {fg='black'},
  symbols = {
    unix = '', -- e712
    dos = '',  -- e70f
    mac = '',  -- e711
  }
}
"
]
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
[
  "{\"location\"}"
]
```

### `vim.statusline.lualine.componentSeparator.left`

Component separator for left side

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>string</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>""</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
""
```

### `vim.statusline.lualine.componentSeparator.right`

Component separator for right side

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>string</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>""</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
""
```

### `vim.statusline.lualine.enable`

Whether to enable lualine statusline plugin.

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>false</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix), [`conf/vim-options.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/vim-options.nix)</span>

</div>

**Value:**

```nix
true
```

### `vim.statusline.lualine.sectionSeparator.left`

Section separator for left side

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>string</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>""</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
""
```

### `vim.statusline.lualine.sectionSeparator.right`

Section separator for right side

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>string</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>""</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/lualine.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/lualine.nix)</span>

</div>

**Value:**

```nix
""
```
