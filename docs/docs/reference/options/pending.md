---
title: Not yet generated
sidebar_position: 1
---

# Not yet generated

This directory is filled by `bun run gen`, which evaluates the flake's
`options-json` output and renders one page per `conf/` file (see the docs
site README). That renderer does not exist yet — it lands in a later task.

This placeholder exists only so Docusaurus's `autogenerated` sidebar item
for `reference/options` has something to pick up; the directory may not be
empty or missing. **Delete this file** once the renderer produces real
content here.
