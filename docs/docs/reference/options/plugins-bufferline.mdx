---
title: bufferline
sidebar_label: bufferline
sidebar_position: 7
description: Options set by conf/plugins/bufferline.nix
---

# `conf/plugins/bufferline.nix`

{/* This page is generated by docs/scripts/render-options.ts. Do not edit it by hand. */}

This page is generated from a Nix evaluation of the configuration. It lists the 2 options that [`conf/plugins/bufferline.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/bufferline.nix) sets.

### `vim.tabline.nvimBufferline.enable`

Whether to enable neovim bufferline.

<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>boolean</code></span>

<span class="option-meta__item"><strong>Default:</strong> <code>false</code></span>

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/bufferline.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/bufferline.nix)</span>

</div>

**Value:**

```nix
true
```

### `vim.tabline.nvimBufferline.setupOpts`

Option table to pass into the setup function of bufferline-nvim

You can pass in any additional options even if they're
not listed in the docs


<div class="option-meta">

<span class="option-meta__item"><strong>Type:</strong> <code>open submodule of anything</code></span>

<span class="option-meta__item"><strong>Default:</strong></span>

```nix
{ }
```

<span class="option-meta__item"><strong>Defined in:</strong> [`conf/plugins/bufferline.nix`](https://github.com/ItzEmoji/nvim/blob/main/conf/plugins/bufferline.nix)</span>

</div>

**Value:**

```nix
{
  highlights = { };
  options = {
    always_show_bufferline = true;
    auto_toggle_bufferline = true;
    buffer_close_icon = " 󰅖 ";
    close_command = mkLuaInline ''
      function(bufnum)
        require("bufdelete").bufdelete(bufnum, false)
      end
    '';
    close_icon = "  ";
    color_icons = true;
    custom_filter = null;
    diagnostics = false;
    diagnostics_indicator = mkLuaInline ''
      function(count, level, diagnostics_dict, context)
        local s = " "
          for e, n in pairs(diagnostics_dict) do
            local sym = e == "error" and "   "
              or (e == "warning" and "   " or "  " )
            s = s .. n .. sym
          end
        return s
      end
    '';
    diagnostics_update_in_insert = false;
    duplicates_across_groups = true;
    enforce_regular_tabs = false;
    get_element_icon = null;
    hover = {
      delay = 200;
      enabled = true;
      reveal = [
        "close"
      ];
    };
    indicator = {
      icon = null;
      style = "underline";
    };
    left_mouse_command = "buffer %d";
    left_trunc_marker = "";
    max_name_length = 18;
    max_prefix_length = 15;
    middle_mouse_command = null;
    mode = "tabs";
    modified_icon = "● ";
    move_wraps_at_ends = false;
    name_formatter = null;
    numbers = "none";
    offsets = [
      {
        filetype = "NvimTree";
        highlight = "Directory";
        separator = true;
        text = "File Explorer";
      }
      {
        filetype = "neo-tree";
        highlight = "Directory";
        separator = true;
        text = "File Explorer";
      }
      {
        filetype = "snacks_layout_box";
        highlight = "Directory";
        separator = true;
        text = "File Explorer";
      }
    ];
    persist_buffer_sort = true;
    right_mouse_command = "vertical sbuffer %d";
    right_trunc_marker = "";
    separator_style = "thin";
    show_buffer_close_icons = true;
    show_buffer_icons = true;
    show_close_icon = true;
    show_duplicate_prefix = true;
    show_tab_indicators = true;
    sort_by = "extension";
    style_preset = mkLuaInline ''
      require('bufferline').style_preset.default
    '';
    tab_size = 18;
    themable = true;
    truncate_names = true;
  };
}
```
