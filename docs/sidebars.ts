import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docsSidebar: [
    'intro',
    // Sits above the guides deliberately: "what are the keys?" is the question
    // this site gets asked most, and it is one generated page.
    'reference/keybindings',
    {
      type: 'category',
      label: 'Guides',
      items: [{type: 'autogenerated', dirName: 'guides'}],
    },
    {
      type: 'category',
      label: 'Option Reference',
      link: {
        type: 'generated-index',
        description:
          'Every nvf option this configuration sets, generated from a live Nix evaluation.',
      },
      items: [{type: 'autogenerated', dirName: 'reference/options'}],
    },
  ],
};

export default sidebars;
